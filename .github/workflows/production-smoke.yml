# Production verification guard for NexusNova core, HumanProof, Intelligence, Labs and trust/tool pages.
name: NexusNova Production Smoke

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/production-smoke.yml'
      - 'index.html'
      - 'nova-intelligence.html'
      - 'assets/css/nova-discoverability.css'
      - 'assets/js/nova-discoverability.js'
      - 'humanproof.html'
      - 'live.html'
      - 'sports-live.html'
      - 'network-tools.html'
      - 'public-ip-checker.html'
      - 'ssl-certificate-checker.html'
      - 'currency-rates.html'
      - 'gold-rates.html'
      - 'fuel-rates.html'
      - 'weather-live.html'
      - 'earthquakes-live.html'
      - 'zakat-calculator-pakistan.html'
      - 'electricity-bill-calculator-pakistan.html'
      - 'salary-tax-calculator-pakistan.html'
      - 'prayer-times-qibla-pakistan.html'
      - 'labs.html'
      - 'pulse.html'
      - 'magic-drop.html'
      - 'xray.html'
      - 'widgets.html'
      - 'widget-pakistan-today.html'
      - 'reaction-time-test.html'
      - 'contact.html'
      - 'privacy.html'
      - 'resume-builder.html'
      - 'image-to-text-ocr.html'
      - 'invoice-maker.html'
      - 'jpg-to-pdf.html'
      - 'ai-prompt-builder.html'
      - 'gaming-sensitivity-converter.html'
      - 'invoice-maker.html'
      - 'typing-speed-test.html'
      - 'photo-cctv-enhancer.html'
      - 'reaction-time-test.html'
      - 'gaming-settings-notes.html'
      - 'image-resizer.html'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  production-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 7
    steps:
      - name: Wait for deployed flagship and trust markers
        shell: bash
        run: |
          set -euo pipefail
          base='https://nexusnovatools.com'
          for attempt in $(seq 1 30); do
            home="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/?smoke=${GITHUB_SHA}-${attempt}" || true)"
            intelligence="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/nova-intelligence.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            humanproof="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/humanproof.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            live="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/live.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            labs="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/labs.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            contact="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/contact.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            privacy="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/privacy.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            resume="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/resume-builder.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            ocr="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/image-to-text-ocr.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            invoice="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/invoice-maker.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            jpgpdf="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/jpg-to-pdf.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            aiprompt="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/ai-prompt-builder.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            sens="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/gaming-sensitivity-converter.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            invoice_tool="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/invoice-maker.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            typing="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/typing-speed-test.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            cctv="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/photo-cctv-enhancer.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            reaction="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/reaction-time-test.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            settings="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/gaming-settings-notes.html?smoke=${GITHUB_SHA}-${attempt}" || true)"
            resizer="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base/image-resizer.html?smoke=${GITHUB_SHA}-${attempt}" || true)"

            if grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/">' <<<"$home" \
              && grep -Fq 'NexusNova HumanProof' <<<"$home" \
              && grep -Fq 'id="trust"' <<<"$home" \
              && grep -Fq 'humanproof.html#live-verification' <<<"$home" \
              && grep -Fq 'Nova Intelligence' <<<"$home" \
              && grep -Fq 'href="/nova-intelligence.html"' <<<"$home" \
              && grep -Fq 'Try Nova Intelligence →' <<<"$home" \
              && grep -Fq 'Tell Nova what you need.' <<<"$home" \
              && grep -Fq 'NOVA INTELLIGENCE • FLAGSHIP DISCOVERY' <<<"$home" \
              && grep -Fq '<meta name="robots" content="noindex,nofollow">' <<<"$intelligence" \
              && grep -Fq 'GENERAL-PURPOSE INTELLIGENCE' <<<"$intelligence" \
              && grep -Fq 'NexusNova Intelligence — AI Workspace' <<<"$intelligence" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/humanproof.html">' <<<"$humanproof" \
              && grep -Fq 'exact-action binding' <<<"$humanproof" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/live.html">' <<<"$live" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/labs.html">' <<<"$labs" \
              && grep -Fq 'nexusnovatools@gmail.com' <<<"$contact" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/privacy.html">' <<<"$privacy" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/resume-builder.html">' <<<"$resume" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/image-to-text-ocr.html">' <<<"$ocr" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/invoice-maker.html">' <<<"$invoice" \
              && grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/jpg-to-pdf.html">' <<<"$jpgpdf" \
              && grep -Fq 'Prompt review before you send it' <<<"$aiprompt" \
              && grep -Fq 'Key Features &amp; Practical Applications' <<<"$aiprompt" \
              && grep -Fq 'Frequently Asked Questions' <<<"$aiprompt" \
              && ! grep -Fq 'Guide on How to Use the AI Prompt Builder' <<<"$aiprompt" \
              && ! grep -Fq 'Enter Universe Atlas' <<<"$labs" \
              && grep -Fq 'Open Nova Intelligence' <<<"$labs" \
              && grep -Fq 'data-flagship-depth' <<<"$sens" \
              && grep -Fq 'data-flagship-depth' <<<"$invoice_tool" \
              && grep -Fq 'data-flagship-depth' <<<"$typing" \
              && grep -Fq 'data-flagship-depth' <<<"$cctv" \
              && grep -Fq 'data-flagship-depth' <<<"$reaction" \
              && grep -Fq 'data-flagship-depth' <<<"$settings" \
              && grep -Fq 'data-flagship-depth' <<<"$resizer"; then
              echo 'Production smoke PASS: core trust/product markers and recovery changes are exposed publicly.'
              exit 0
            fi
            sleep 5
          done

          echo '--- ORIGIN VS CUSTOM-DOMAIN DIAGNOSTIC ---'
          origin_labs="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "https://fahadsoomro123.github.io/nexusnova-website/labs.html?smoke=${GITHUB_SHA}" || true)"
          origin_ai="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "https://fahadsoomro123.github.io/nexusnova-website/ai-prompt-builder.html?smoke=${GITHUB_SHA}" || true)"
          origin_hp="$(curl -fsSL --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "https://fahadsoomro123.github.io/nexusnova-website/humanproof.html?smoke=${GITHUB_SHA}" || true)"
          echo "origin_labs bytes=${#origin_labs} nova=$(grep -Fq 'Open Nova Intelligence' <<<"$origin_labs" && echo PASS || echo FAIL)"
          echo "origin_ai bytes=${#origin_ai} prompt=$(grep -Fq 'Prompt review before you send it' <<<"$origin_ai" && echo PASS || echo FAIL)"
          echo "origin_hp bytes=${#origin_hp} canonical=$(grep -Fq '<link rel="canonical" href="https://nexusnovatools.com/humanproof.html">' <<<"$origin_hp" && echo PASS || echo FAIL)"
          if grep -Fq '<link rel="canonical"' <<<"$humanproof"; then grep -oi '<link rel="canonical"[^>]*>' <<<"$humanproof" | head -1; else echo 'custom_humanproof_canonical=NOT_FOUND'; fi

          echo '--- CORE MARKER DIAGNOSTIC ---'
          checks=(
            "home_canonical|$home|<link rel="canonical" href="https://nexusnovatools.com/">"
            "home_humanproof|$home|NexusNova HumanProof"
            "home_trust|$home|id="trust""
            "home_live_verify|$home|humanproof.html#live-verification"
            "home_nova|$home|Nova Intelligence"
            "home_nova_href|$home|href="/nova-intelligence.html""
            "home_nova_cta|$home|Try Nova Intelligence →"
            "home_nova_prompt|$home|Tell Nova what you need."
            "home_nova_marker|$home|NOVA INTELLIGENCE • FLAGSHIP DISCOVERY"
            "intel_noindex|$intelligence|<meta name="robots" content="noindex,nofollow">"
            "intel_flow|$intelligence|UNDERSTAND → ROUTE → ACT"
            "intel_title|$intelligence|NexusNova Intelligence — Product Preview"
            "hp_canonical|$humanproof|<link rel="canonical" href="https://nexusnovatools.com/humanproof.html">"
            "hp_binding|$humanproof|exact-action binding"
          )
          for check in "${checks[@]}"; do
            name="${check%%|*}"
            rest="${check#*|}"
            body="${rest%%|*}"
            marker="${rest#*|}"
            if grep -Fq "$marker" <<<"$body"; then echo "PASS $name"; else echo "FAIL $name"; fi
          done

          echo '--- LIVE PRODUCTION DIAGNOSTIC ---'
          for item in home intelligence humanproof live labs contact privacy resume ocr invoice jpgpdf aiprompt sens invoice_tool typing cctv reaction settings resizer; do
            value="${!item}"
            printf '%-14s bytes=%s' "$item" "${#value}"
            case "$item" in
              labs) if grep -Fq 'Open Nova Intelligence' <<<"$value"; then echo ' marker=nova-intelligence:PASS'; else echo ' marker=nova-intelligence:FAIL'; fi
                   if grep -Fq 'Enter Universe Atlas' <<<"$value"; then echo '  legacy-universe-atlas:FOUND'; else echo '  legacy-universe-atlas:ABSENT'; fi ;;
              aiprompt) if grep -Fq 'Prompt review before you send it' <<<"$value"; then echo ' marker=prompt-review:PASS'; else echo ' marker=prompt-review:FAIL'; fi
                        if grep -Fq 'Guide on How to Use the AI Prompt Builder' <<<"$value"; then echo '  legacy-duplicate-guide:FOUND'; else echo '  legacy-duplicate-guide:ABSENT'; fi ;;
              sens|invoice_tool|typing|cctv|reaction|settings|resizer)
                if grep -Fq 'data-flagship-depth' <<<"$value"; then echo ' marker=flagship-depth:PASS'; else echo ' marker=flagship-depth:FAIL'; fi ;;
              *) echo ;;
            esac
          done
          echo 'Production did not expose all expected current public markers.' >&2
          exit 1

      - name: Verify important production routes
        shell: bash
        run: |
          set -euo pipefail
          base='https://nexusnovatools.com'
          routes=(
            '/'
            '/nova-intelligence.html'
            '/humanproof.html'
            '/live.html'
            '/sports-live.html'
            '/network-tools.html'
            '/public-ip-checker.html'
            '/ssl-certificate-checker.html'
            '/currency-rates.html'
            '/gold-rates.html'
            '/fuel-rates.html'
            '/weather-live.html'
            '/earthquakes-live.html'
            '/zakat-calculator-pakistan.html'
            '/electricity-bill-calculator-pakistan.html'
            '/salary-tax-calculator-pakistan.html'
            '/prayer-times-qibla-pakistan.html'
            '/labs.html'
            '/pulse.html'
            '/magic-drop.html'
            '/xray.html'
            '/widgets.html'
            '/widget-pakistan-today.html'
            '/reaction-time-test.html'
            '/contact.html'
            '/privacy.html'
            '/resume-builder.html'
            '/image-to-text-ocr.html'
            '/invoice-maker.html'
            '/jpg-to-pdf.html'
          )
          for route in "${routes[@]}"; do
            code="$(curl -sS -L -o /dev/null -w '%{http_code}' --connect-timeout 10 --max-time 20 -H 'Cache-Control: no-cache' "$base$route?smoke=${GITHUB_SHA}")"
            echo "$code $route"
            test "$code" = '200'
          done
