// Approved Nova Drive + Vehicle Tracking owner UI.
// The implementation lives in one module so the legacy overlay cannot conflict.
import './nova-drive-approved-ui-v2.js?ota=drive-original-png-v1';
